query Query($poolAddress: String!, $chainId: Int!, $timeSpacing: Int!, $chartForToken: Int!) {
 candles(poolAddress: $poolAddress, chainId: $chainId, chartForToken: $chartForToken,timeSpacing: $timeSpacing) {
  uuid,
  open,
  close,
  low,
  high,
  timestamp,
  amountSwaps
 }
}
